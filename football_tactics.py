import matplotlib.pyplot as plt\nimport numpy as np\n\ndef plot_formation(formation):\n    fig, ax = plt.subplots(figsize=(10, 7))\n    ax.set_xlim(0, 10)\n    ax.set_ylim(0, 7)\n    ax.set_xticks([])\n    ax.set_yticks([])\n    ax.set_title('Football Formation Visualization')\n\n    for position in formation:\n        x, y = position['coords']\n        ax.text(x, y, position['name'], fontsize=12, ha='center', va='center')\n        ax.add_patch(plt.Circle((x, y), 0.3, color='blue', alpha=0.5))\n\n    plt.grid(which='both')\n    plt.show()\n\nformation = [\n    {'name': 'Goalkeeper', 'coords': (5, 1)},\n    {'name': 'Defender 1', 'coords': (3, 2)},\n    {'name': 'Defender 2', 'coords': (5, 2)},\n    {'name': 'Defender 3', 'coords': (7, 2)},\n    {'name': 'Midfielder', 'coords': (4, 4)},\n    {'name': 'Forward', 'coords': (5, 6)}\n]\n\nplot_formation(formation)